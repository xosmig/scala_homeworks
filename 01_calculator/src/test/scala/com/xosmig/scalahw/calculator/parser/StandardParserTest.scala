package com.xosmig.scalahw.calculator.parser

import com.xosmig.scalahw.calculator.lexer.ValueToken
import org.junit.Assert._
import org.junit.Test

class StandardParserTest {
  @Test
  def ParsesSingleValue(): Unit = {
//    assertEquals(42, StandardParser.parse(List(ValueToken(42, 0))).evaluate())
  }
}
